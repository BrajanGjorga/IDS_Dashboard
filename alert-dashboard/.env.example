DB_HOST=localhost
DB_PORT=5432
DB_NAME=alert_dashboard
DB_USER=alert_dashboard
DB_PASSWORD=

APP_HOST=0.0.0.0
APP_PORT=8001
LOG_LEVEL=INFO

# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
SESSION_SECRET=
# Use true when the dashboard is served over HTTPS.
SESSION_HTTPS_ONLY=false

